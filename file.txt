pipeline {
    agent any // Specifies that the pipeline can run on any available Jenkins agent.

    stages {
        stage('Hello') { // Defines a stage named 'Hello'.
            steps {
                echo 'Hello, Jenkins Pipeline!' // Executes a step to print a message.
            }
        }
    }
}
